name: somtok-live-smoke
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Duplicate smoke workflow disabled; staged QA is in somtok-live-qa.yml"
